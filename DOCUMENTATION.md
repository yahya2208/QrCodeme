# QRme V8 - التوثيق النهائي

## 📌 نظرة عامة

**QRme** هو تطبيق هويات رقمية يعتمد على أكواد QR بشكل كامل.

### المبدأ الأساسي
- ❌ **ليس تطبيق روابط** - لا يتم فتح أي رابط مباشرة
- ✅ **تطبيق QR Codes** - أي ضغط = عرض QR Code فقط

---

## 🏗️ الهيكل المعماري

```
QRme/
├── index.html          # الصفحة الرئيسية
├── js/
│   ├── app.js          # منطق التطبيق الرئيسي (V8)
│   ├── config.js       # إعدادات Supabase
│   ├── i18n.js         # دعم اللغات (AR/EN)
│   └── supabase.js     # عميل Supabase
├── styles/
│   ├── main.css        # الأنماط الرئيسية
│   ├── animations.css  # الحركات
│   └── components.css  # مكونات إضافية
├── backend/
│   └── src/
│       ├── server.js   # خادم Express
│       ├── routes/     # نقاط API
│       └── config/     # إعدادات Supabase
├── database/
│   └── migration_v8.sql # أحدث ترحيل للبيانات
└── mobile_app/         # تطبيق Flutter
```

---

## 🔐 منطق الأمان

### للمستخدمين العاديين (Public View):
| ما يظهر | ما لا يظهر |
|---------|-----------|
| ✅ أيقونة الخدمة | ❌ الرابط الفعلي |
| ✅ اسم الكود | ❌ القيمة المشفرة |
| ✅ رسالة "اضغط لعرض QR" | ❌ أزرار التعديل/الحذف |

عند الضغط: تظهر رسالة "البيانات مشفرة" مع أيقونة 🔒

### للمالك (Owner View):
| ما يظهر | الإجراءات المتاحة |
|---------|------------------|
| ✅ أيقونة الخدمة | ✏️ تعديل الرابط |
| ✅ اسم الكود | 🗑️ حذف الكود |
| ✅ زر QR يعمل | ➕ إضافة كود جديد |

عند الضغط: يظهر QR Code حقيقي قابل للمسح

---

## 📱 تدفق المستخدم

### 1. التسجيل
```
Email + Name + Password + Confirm → Email Verification → تسجيل الدخول
```

### 2. إنشاء الهوية
```
المستخدم المسجل → "تكوين الهوية" → إدخال الاسم والنبذة → الهوية جاهزة
```

### 3. إضافة كود (داخل الهوية فقط)
```
فتح الهوية → "إضافة كود" → اختيار المجال → اختيار الخدمة → إدخال الرابط → حفظ
```

### 4. عرض QR (للمالك)
```
الضغط على الكود → يظهر QR Code كبير وواضح → قابل للمسح
```

### 5. محاولة عرض QR (للآخرين)
```
الضغط على الكود → رسالة "البيانات مشفرة" → لا يظهر QR
```

---

## 🎨 التصميم

### الألوان
- **خلفية**: `#030303` (أسود عميق)
- **Neon Yellow**: `#f0ff42`
- **Glow**: `rgba(240, 255, 66, 0.5)`

### الخطوط
- **العربية**: Cairo
- **الإنجليزية**: Outfit
- **Mono**: Space Mono

### المبادئ
- تصميم مستقبلي مع تأثيرات Glow
- RTL/LTR صحيح 100%
- Smooth animations مع GSAP
- Glassmorphism لبعض العناصر

---

## 🗄️ قاعدة البيانات (Supabase)

### الجداول الرئيسية

| الجدول | الوصف |
|--------|-------|
| `users` | بيانات المستخدمين |
| `nexus_identities` | الهويات الرقمية |
| `shops` | الأكواد/الروابط |
| `code_categories` | المجالات (Social، Store، etc.) |
| `code_services` | الخدمات (Facebook، Instagram، etc.) |

### RLS (Row Level Security)
- ✅ المستخدم يرى هوياته فقط للتعديل
- ✅ الجميع يرى الهويات العامة للقراءة
- ✅ الروابط لا تظهر إلا للمالك عبر RPC

### RPC Functions
- `get_identity_codes(identity_id, viewer_id)` - جلب الأكواد مع التحقق من الملكية
- `get_public_hub(limit)` - جلب الهويات العامة
- `create_user_identity(name, bio)` - إنشاء هوية جديدة

---

## 🚀 التشغيل

### Frontend
```bash
cd QRme
npm install
npx serve -l 5500 .
# يفتح على http://localhost:5500
```

### Backend
```bash
cd backend
npm install
npm start
# يعمل على http://localhost:3001
```

---

## ✅ قائمة التحقق

- [x] تسجيل مستخدم جديد مع تأكيد البريد
- [x] إنشاء هوية رقمية
- [x] إضافة أكواد من داخل الهوية فقط
- [x] عرض QR Code عند الضغط (للمالك)
- [x] رسالة مشفرة للآخرين
- [x] تعديل الروابط (للمالك فقط)
- [x] حذف الأكواد (للمالك فقط)
- [x] RTL صحيح للعربية
- [x] LTR صحيح للإنجليزية
- [x] اتصال Supabase سحابي
- [x] RLS للأمان
- [x] تصميم Black × Yellow

---

## ⚠️ ملاحظات هامة

1. **الروابط لا تُفتح أبداً** - التطبيق يعرض QR فقط
2. **لا تخزين محلي** - كل البيانات في Supabase
3. **الأكواد تُنشأ من داخل الهوية فقط**
4. **أي نص عربي معكوس = BUG يجب إصلاحه**

---

## 📞 API Endpoints

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/health` | فحص صحة الخادم |
| GET | `/api/nexus/discovery` | الهويات العامة |
| GET | `/api/nexus/:id` | تفاصيل هوية |
| POST | `/api/nexus/create` | إنشاء هوية |

---

**الإصدار**: V8 Final  
**التاريخ**: 2026-01-27
